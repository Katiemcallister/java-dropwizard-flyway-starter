CREATE TABLE `Client` (
                            `client_id` smallint primary key auto_increment,
                            `name` varchar(255),
                            `address` varchar(200),
                            `phone_num` varchar(64),
                            `proj_id` smallint

);
